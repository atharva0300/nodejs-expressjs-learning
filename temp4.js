require('./3-Modules-3');

// notice that, even when we have not called 
// the addValues function of the app.js 
// the function still gets executed 

// because, it first executes it , then imports it