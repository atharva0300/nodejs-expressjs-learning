const items = ['item1' , 'item2'];

const person = {
    name : 'atharva'
}

// we can export only one thing at a time 

// exporting object person
module.exports = {person};

// exporting the items array 
//modules.exports = {items};